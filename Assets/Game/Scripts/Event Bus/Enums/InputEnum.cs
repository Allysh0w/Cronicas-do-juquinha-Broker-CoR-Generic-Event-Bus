using UnityEngine;


// refactor this code later because I need to build the input instructions based on game jam code
public enum InputEnum
{
    Move,
    Slash,
    ToggleSkillTree,
    ToggleStatusUI,
    TalkDialogue
}
